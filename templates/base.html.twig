<!DOCTYPE html>

<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{% endblock %}</title>
        {% block stylesheets %} 
						<link rel="stylesheet" href="{{asset("build/css/materialize.min.css")}}">
  			{% endblock %}
    </head>
    <body>	
    	<header>
        		  <div class="navbar-fixed">
				    <nav>
				      <div class="nav-wrapper">
				        <a href="{{ path('home') }}" class="brand-logo">Logo</a>
				        <ul class="right hide-on-med-and-down">
                   <ul class="right hide-on-med-and-down">
                  <li><a href="{{ path('logout') }}">Se déconnecter</a></li>
               
                </ul>
				          <li><a href="{{ path('materiel.index') }}">Matériels</a></li>
				       
				        </ul>
               
				      </div>
				    </nav>
 				 </div>
        	</header>
        {% block body %}{% endblock %}
        {% block javascripts %}
            <script src="{{asset('build/js/jquery-3.3.1.min.js')}}"></script>
						<script src="{{asset('build/js/materialize.min.js')}}"></script>
        {% endblock %}

  		
    </body>
</html>
