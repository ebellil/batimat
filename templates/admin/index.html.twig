{% extends 'base.html.twig' %}

{% block title %}Hello welcome Admin!{% endblock %}
{% block body %}
<table class="table table-hover table-responsive">
    <thead>       
            <tr>
                <th>id</th>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Mot de passe</th>
                <th>Options</th>
            </tr>
    </thead>

    <tbody>
        {% for admin in admin %}
                <tr>
                    <td>{{admin.id}}</td>
                    <td>{{admin.nom}}</td>
                    <td>{{admin.prenom}}</td>
                    <td>{{admin.mdp}}</td>
                    <td>
                        <a href="#" class="btn btn-default btn-small">Editer</a>
                        <a href="#" class="btn btn-danger btn-small">Supprimer</a>
                    </td>
                </tr>
        {% endfor %}
    </tbody>

</table>
</section>

{% endblock %}
