{% extends 'baseAdmin.html.twig' %}

{% block title 'Gérer les admin général' %}

{% block body %}
	<div class="container">
		<h1>Gérer les admin général</h1>
		
		{% for message in app.flashes('success') %}
			<div class="card-panel green accent-3">
				{{ message }}
			</div>
		{% endfor %}

		<div>
			<table>
        		<thead>
          			<tr>
              			<th>Libellé</th>
              			<th>Actions</th>
          			</tr>
        		</thead>
        		<tbody>
        			{% for adminGeneral in adminGenerals %}
			        <tr>
			            <td>{{adminGeneral.username}}</td>
			            <td>
			            	<a href="{{ path('admin.adminGeneral.edit', {id: adminGeneral.id}) }}" class="waves-effect waves-light btn">Editer</a>
			            </td>
			            <td>
			            	<form method="post" action="{{ path('admin.adminGeneral.delete', {id: adminGeneral.id}) }}">
			            		<input type="hidden" name="_method" value="DELETE">
			            		<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adminGeneral.id) }}">
			            		<button class="btn waves-effect waves-light">Supprimer</button>
			            	</form>
			            </td>
			        </tr>
			        {% endfor %}
        		</tbody>
      		</table>
      		<div class="row">
      			<a href="{{ path('admin.adminGeneral.new') }}" class="waves-effect waves-light btn">Ajouter</a>
      		</div>
		</div>
	</div>


{% endblock %}